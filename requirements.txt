# Core dependencies for the HCNG combustion analysis
# Install with:  pip install -r requirements.txt
# (Conda:        conda install -c cantera cantera numpy matplotlib)

cantera>=3.0
numpy>=1.23
matplotlib>=3.6
